<Window x:Class="SnakeVoiceControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SnakeVoiceControl"
        mc:Ignorable="d"
        Title="SnakeGame" Height="480" MinHeight="200" Width="720" MinWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Viewbox Grid.Row="0" Grid.Column="0" Width="{Binding}" Height="{Binding}" Stretch="Fill">
            <Canvas Name="canvas" Background="Brown" Width="740" Height="500"/>
        </Viewbox>
    </Grid>
</Window>
